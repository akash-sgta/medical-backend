# ========================================================================
from django.urls import re_path


# ========================================================================

urlpatterns = [
    # re_path(
    #     r"master/text/(?P<pk>\d*)$",
    #     Text.as_view(),
    #     name="Master_Text",
    # ),
]
