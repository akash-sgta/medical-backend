# ========================================================================
from django.apps import AppConfig

# ========================================================================


class AppCdnConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "app_cdn"
